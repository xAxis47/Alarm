# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'Alarm' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

	pod 'SwiftDate'

  # Pods for Alarm

  target 'AlarmTests' do
    inherit! :search_paths

	pod 'SwiftDate'

    # Pods for testing
  end

  target 'AlarmUITests' do

	pod 'SwiftDate'

    # Pods for testing
  end

end
